[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".claude/skills/facebook/scripts"]
addopts = "-m 'not live'"
markers = ["live: reads the real logged-in account"]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
