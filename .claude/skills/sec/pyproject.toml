[project]
name = "sec-skill"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["edgartools==5.58.0", "python-dotenv==1.2.3", "httpx>=0.28,<0.29", "lxml>=5,<7", "beautifulsoup4>=4.13,<5"]

[dependency-groups]
dev = ["pytest>=8,<10", "ruff>=0.12,<1"]

[tool.uv]
default-groups = []

[tool.ruff]
line-length = 120
